Dependencies:

  - pip install -r requirements.txt
  - The above command installs pip install openai-whisper sentence-transformers chromadb ffmpeg-python
  - ffmpeg  must be installed on the system